import React from 'react';
import styled from 'styled-components';

import { theme } from './theme';

export const CardList = styled.div`
  display: flex;
  flex-wrap: wrap;
  margin-top: ${theme.space.standard}px;
`;
